[build-system]
requires = ["setuptools", "wheel", "pybind11>=2.5.0"]
build-backend = "setuptools.build_meta"

[tool.black]
line-length = 80

[tool.pytest.ini_options]
addopts = "-ra -q -s"
testpaths = [
    "pyhfp/tests"
]